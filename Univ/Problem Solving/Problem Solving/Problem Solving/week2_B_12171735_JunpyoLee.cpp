#include <iostream>
#include <string>
using namespace std;
int main() {
	iostream::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);
	int T, M, N;
	cin >> T;
	// 이전에 cin으로 입력받았으면
	// cin.ignore()후에 getline 사용
	while (T--) {
		string command;
		cin >> M, N;
		
		
		
	}
	


}