#include <iostream>
#include <stack>

using namespace std;
int K;
stack<int> col_1, col_2, col_3;

int hanoi(int N, int start, int dest, int middle, int move){ 
	
	hanoi(N-1, )
}

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);
	int T, N;

	cin >> T;
	while (T--) {
		cin >> N >> K;

		while (!col_1.empty()) col_1.pop();
		while (!col_2.empty()) col_2.pop();
		while (!col_3.empty()) col_3.pop();
	}
	return 0;
}